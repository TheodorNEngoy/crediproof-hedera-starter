# CrediProof — One‑Pager

• On-chain audit with **HCS** (immutable timestamped message).
• Tokenization with **HTS** (2 decimals), transfer, settle, **burn**.
• Judges verify on **HashScan** by pasting Topic/Token/Account IDs.

Next steps: DID/VC issuance; OCR to JSON; automated settlement rails.
